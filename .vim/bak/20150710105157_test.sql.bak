
-- +goose Up
-- SQL in section 'Up' is executed when this migration is applied
BEGIN;
ALTER TABLE TAordering_information_component SET comasdsadaponent_id = 5;
COMMIT;
-- +goose Down
-- SQL section 'Down' is executed when this migration is rolled back

ALTER TABLE TAordering_information_component SET comasdsadaponent_id = 5;
