{% from 'macros.jinja2' import basic_field, checkbox_field, append_field, prepend_field %}

<div class="row">
  <div class="col-md-5">{{ append_field(form.interior_dimension.interior_depth, 'in') }}</div>
  <div class="col-md-5">{{ append_field(form.interior_dimension.interior_height, 'in') }}</div>
</div>
<div class="row">
  <div class="col-md-5">{{ append_field(form.interior_dimension.interior_width, 'in') }}</div>
</div>
